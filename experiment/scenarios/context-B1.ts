// Order management system
// The function should handle different order statuses

type OrderStatus = 'draft' | 'pending' | 'confirmed' | 'shipped' | 'delivered';

export function getOrderDisplay(status: OrderStatus) {
  // TODO: add display properties for each status
}
